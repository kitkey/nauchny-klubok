Тип сущностей: {{ type }}

Упоминания (имена в ответе используй ДОСЛОВНО); под каждым — контекст из статьи:
{% for e in entities %}- "{{ e.name }}"{% if e.aliases %} (варианты: {{ e.aliases | join("; ") }}){% endif %}
    контекст: {{ e.context | default("") }}
{% endfor %}